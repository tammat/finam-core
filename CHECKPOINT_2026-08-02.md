# MarketCore checkpoint — 2 August 2026

## Зафиксированная цель

Довести один и тот же заранее определённый эксперимент до проверяемого исхода,
не меняя кандидатов по текущему P&L и не расширяя вселенную.

Заморожены четыре V5-ветки:

1. `SBER@MISX LONG`;
2. `BRQ6@RTSX SHORT`;
3. `GDU6@RTSX / GLDRUBF@RTSX LONG`;
4. `CNYRUBF@RTSX LONG`.

Газ остаётся резервным Shadow. Новая когорта создаётся только при доказанном
изменении определения данных, а не при неудобном результате.

## Что исправлено

- Исторический `R` приведён к денежным единицам: начальный риск учитывает
  стоимость пункта фьючерса или размер лота акции. При отсутствии спецификации
  метрика не вычисляется.
- Recovery shock-gate считает сессию фьючерсов с 08:50, акций — с 06:50.
- `EIA_GAS_STORAGE` распознаётся как событие запасов.
- Добавлен вероятностный `RegimeStateV1`: `TREND/RANGE/SHOCK`.
- Смена устойчивого режима требует трёх последовательных подтверждений с
  вероятностью не ниже 60%.
- Выполнен причинный replay 279 контекстов; подтверждено 4 смены режима.
- Последний исторический контекст: trend 84,5%, range 12,7%, shock 2,8%.
- UI показывает вероятности, устойчивый режим и текущего кандидата режима.

## Проверенное состояние

- `finam-research-runtime.service` активен после рестарта 22:53 МСК.
- Последний исследовательский цикл: `OK`, 7 символов, ошибок 0, изменений 0.
- Целевой набор: 31 тест успешно.
- Сегодня сигналов MOEX: 0; закрытых сделок: 0; P&L: 0 ₽.
- 3514 сегодняшних свечей относятся только к круглосуточным BTCUSD/ETHUSD.
- V5: четыре run `COLLECTING`, включено 0, исключено 0.
- ExecutionSpec parity: четыре `NOT_PROVEN`; observation parity: 0.
- REAL и автоматическое продвижение по вероятностному режиму выключены.

## Правило продолжения

До результата запрещены расширение инструментов, новая оптимизационная сетка и
ручная замена кандидата. После первых 5–10 независимых событий проверяется только
достижимость и качество данных. Статистический PASS возможен только после
заданного OOS-минимума, издержек, placebo и parity `MATCH`.

Допустимый технический откат:

- дефект или утечка данных;
- несовпадение Research и Runtime;
- неверная контрактная спецификация или единицы риска;
- нарушение причинности свечей;
- защитный risk-gate;
- OOS FAIL по заранее заданным критериям.

Подгонка параметров после отдельной убыточной сделки не допускается.
